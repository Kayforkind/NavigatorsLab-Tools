/* agents.html — static documentation page; the entry exists so Vite bundles
 * the shared stylesheet pipeline. All content is server-static; no behavior.
 * (Deep links documented here are exercised by scripts/e2e.cjs instead.) */
export {};
